# Obsidian Vault 规范

## 目录结构

```
笔记/
├── .claude/             # Claude 配置
├── .obsidian/           # Obsidian 配置
├── 概念/                # 静态定义 — "是什么"
│   ├── 基础/            # 基础概念（抽象化、具象化、神经网络）
│   └── 认知功能/        # 人类先天认知能力（归纳、演绎、注意力等）
├── 方法/                # 操作策略 — "怎么做"
├── 现象或规律/          # 关系与过程 — 可观察的规律/现象
└── *.md                 # 根级笔记（主体、设计、需求等）
```

## Frontmatter 规范

### 标签（tags）

**所有标签使用中文短标签**，不要使用英文/路径式标签（如 `method/priority` ✗）。

| 目录 | 可用标签 | 说明 |
|------|---------|------|
| `概念/基础/` | `基础` | 抽象化、具象化等底层概念 |
| `概念/认知功能/` | `先天` + `简单`/`复杂涌现` | 先天能力 + 复杂度分级 |
| `概念/`（其他） | `基础` 或其他 | 根级概念 |
| `方法/` | `方法`（必选），可选 `先天`/`复杂涌现` | 方法本身是操作策略，但有些方法依赖先天能力或属于复杂涌现 |
| `现象或规律/` | `规律` | 可观察的规律与现象，不是方法也不是概念定义 |

### 别名字段（aliases）

```yaml
aliases:
  - 中文名称
  - English Name
```

### 创建日期（created）

```yaml
created: YYYY-MM-DD
```

### 完整示例

```yaml
---
tags:
  - 基础
aliases:
  - 抽象化
  - Abstraction
created: 2026-06-25
---
```

> **所有笔记必须有 `tags`、`aliases`、`created` 三个字段，缺一不可。**

## 链接规范

- 内部链接使用 Wikilink 格式：`[[笔记名]]`
- 跨目录链接：`[[目录/笔记名]]`
- **不要创建不必要的链接** — 不相关的笔记之间不要强行关联（如方法与概念之间的对比），避免增加混乱。

## 方法笔记内容模板

- **核心思想** — 一句话说透
- **操作方式**（可选）— 怎么做
- **适用场景**（可选）— 什么时候用

## 编辑约定

- 不破坏已有的 frontmatter 字段
- 不添加多余标签或链接
- 不要随意移动或重命名已有笔记
- 内容以 Markdown 为主，可嵌入 Mermaid 图表

## 核心原则

- **方法不是万能解**。任何方法都是启发式的（heuristic），在特定上下文有效，换一个场景可能失效。没有能解决一切问题的方法。
- **关键点很难抓对**。大多数问题真正的困难不在于"用什么方法"，而在于你很可能抓错了关键点，而且自己不知道。方法无法替你发现你没看到的东西。
